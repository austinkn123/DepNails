import { Typography } from '@mui/material';

const Appointment = () => {
    return (
        <div>
            <Typography variant="h3" component="h2">
                Appointment
            </Typography>
        </div>
    );
}

export default Appointment;